version: '3.8'

services:
  db:
    image: postgres:11
    volumes:
      - ./data:/var/lib/postgresql/data/
    ports:
      - 54321:5432
    environment:
      - POSTGRES_USER=test
      - POSTGRES_PASSWORD=test
      - POSTGRES_DB=fulexpro
volumes:
  pg_data:
